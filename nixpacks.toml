# Disable release phase - run migrations at runtime instead
[phases.release]
cmds = ["echo 'Skipping migrations during build'"]

[start]
cmd = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi"
